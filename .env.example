# =====================================
# STEAM BUNDLE API - CONFIGURAÇÃO SIMPLIFICADA V5.3
# =====================================
NODE_ENV=production
PORT=3000
TIMEZONE=America/Sao_Paulo

# API Key para proteger endpoints administrativos
# Gere uma chave segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=sua_api_key_super_secreta_aqui

# =====================================
# CONFIGURAÇÕES DA API DE STORAGE BACKEND
# =====================================
STORAGE_API_URL=https://bundleset-api-storage.vercel.app
STORAGE_API_KEY=your_secret_api_key_here
STORAGE_TIMEOUT=30000
STORAGE_MAX_RETRIES=3
STORAGE_CHUNK_SIZE=1000

# =====================================
# CONFIGURAÇÕES OTIMIZADAS (Render Free)
# =====================================
# ⚡ A maioria dos valores agora está otimizada diretamente no código!
# ⚡ Circuit breaker inteligente, fallback API, auto-resume, validação de integridade
# ⚡ Valores padrão seguros: STEAM_API_DELAY=1500ms, PARALLEL_BUNDLES=3, etc.

# URL Externa do Render (opcional - detectada automaticamente)
# RENDER_EXTERNAL_URL=https://steambundleapi.onrender.com

# =====================================
# AJUSTES FINOS OPCIONAIS
# =====================================
# Descomente apenas se precisar ajustar valores específicos:

# Configurações principais do scraping
# STEAM_API_DELAY=1500              # Delay entre requisições (padrão: 1500ms)
# REQUEST_TIMEOUT=20000             # Timeout das requisições (padrão: 20s)
# MAX_RETRIES=3                     # Tentativas por erro (padrão: 3)

# Configurações de coleta básica
# FETCH_BUNDLES_CONCURRENT=5        # Requisições paralelas (padrão: 5)
# FETCH_BUNDLES_DELAY=200           # Delay entre lotes (padrão: 200ms)
# FETCH_BUNDLES_TIMEOUT=15000       # Timeout da coleta básica (padrão: 15s)

