# === CONFIGURAÇÕES RECOMENDADAS POR CENÁRIO ===

# 🧪 TESTE RÁPIDO (para validar otimizações)
# PARALLEL_BUNDLES=5
# STEAM_API_DELAY=400
# SKIP_DETAILS_THRESHOLD=50
# MAX_APPS_PER_BUNDLE=20
# REQUEST_TIMEOUT=8000
# MAX_RETRIES=2

# 🚀 PRODUÇÃO AGRESSIVA (máxima velocidade)
# PARALLEL_BUNDLES=15
# STEAM_API_DELAY=200
# SKIP_DETAILS_THRESHOLD=100
# MAX_APPS_PER_BUNDLE=30
# REQUEST_TIMEOUT=8000
# MAX_RETRIES=2
# APP_BATCH_SIZE=10

# ⚖️ PRODUÇÃO BALANCEADA (recomendado)
PARALLEL_BUNDLES=10
STEAM_API_DELAY=300
SKIP_DETAILS_THRESHOLD=100
MAX_APPS_PER_BUNDLE=30
REQUEST_TIMEOUT=8000
MAX_RETRIES=2
APP_BATCH_SIZE=8

# 🛡️ PRODUÇÃO CONSERVADORA (máxima segurança)
# PARALLEL_BUNDLES=5
# STEAM_API_DELAY=500
# SKIP_DETAILS_THRESHOLD=150
# MAX_APPS_PER_BUNDLE=50
# REQUEST_TIMEOUT=10000
# MAX_RETRIES=3
# APP_BATCH_SIZE=5

# 🔥 PRODUÇÃO ULTRA RÁPIDA (usar com cuidado)
# PARALLEL_BUNDLES=20
# STEAM_API_DELAY=150
# SKIP_DETAILS_THRESHOLD=80
# MAX_APPS_PER_BUNDLE=25
# REQUEST_TIMEOUT=6000
# MAX_RETRIES=1
# APP_BATCH_SIZE=12

# === OUTRAS CONFIGURAÇÕES ===
NODE_ENV=production
PORT=3000
TIMEZONE=America/Sao_Paulo
API_KEY=sua_api_key_super_secreta_aqui
